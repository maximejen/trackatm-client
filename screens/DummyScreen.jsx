import React from "react";
import { View, Text } from "react-native";

const DummyScreen = ({ navigation }) => {
  return (
    <View
      style={{
        alignItems: "center",
        justifyContent: "center",
        width: "100%",
        height: "100%",
        backgroundColor: "#fff",
      }}
    >
      <Text>TRACK ATM DUMMY SCREEN</Text>
    </View>
  );
};

export default DummyScreen;
